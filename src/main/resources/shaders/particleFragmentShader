#version 400 core

in vec2 passTextureCoords;

out vec4 outColor;

uniform vec4 color;
uniform sampler2D textureSampler;

void main(void) {
	outColor = color * texture(textureSampler, passTextureCoords);
}